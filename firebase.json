{
  "database": {
    "rules": "database.rules.json"
  },
  "hosting": {
    "public": "dist/resume-site",
    "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
